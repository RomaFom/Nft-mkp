{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.0",
    "@types/chai": "^4.3.1",
    "chai": "^4.3.6",
    "web3": "^1.7.4"
  },
  "scripts": {
    "start": "npx hardhat node --network hardhat",
    "test": "npx hardhat test",
    "compile-abi-colc-mkp": "solc --optimize --base-path '/' --include-path './node_modules' --abi ./contracts/Marketplace.sol -o build",
    "compile-bin-solc-mkp": "solc --optimize --base-path '/' --include-path './node_modules' --bin ./contracts/Marketplace.sol -o build",
    "compile-go-file-mkp": "abigen --abi=./contracts/build/Marketplace.abi --bin=./contracts/build/Marketplace.bin --pkg=sc_api --out=../../GoMkp/pkg/smart-contracts/MarketplaceSC.go",
    "compile-all-mkp": "npm run compile-abi-colc-mkp && npm run compile-bin-solc-mkp && npm run compile-go-file-mkp",
    "deploy-no-compile": "npx hardhat run scripts/deploy.ts --network localhost",
    "deploy": "npx hardhat clean && npx hardhat compile && npx hardhat run scripts/deploy.ts --network localhost",
    "deploy-rinkeby": "npx hardhat clean && npx hardhat compile && npx hardhat run scripts/deploy.ts --network rinkeby",
    "deploy-goerli": "npx hardhat clean && npx hardhat compile && npx hardhat run scripts/deploy.ts --network goerli"
  }
}
