# Nft-mkp

1. install all dependencies at hardhat use npm install at client use yarn
# Start blockchain
run locally blockchain

    npm start
deploy contracts

    npm run deploy

myListed contracts 

    npm run myListed
